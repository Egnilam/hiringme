{% extends 'base.html.twig' %}

{% block title %}Update new wishlist{% endblock %}

{% block content %}
    {% block body %}
        <h1>Update wishlist</h1>
        <a href="{{ path('wishlist_list') }}">Back to wishlists</a>
        {{ form_start(form)}}
        {{ form_widget(form) }}
        <button type="submit">Envoyer</button>
        {{ form_end(form) }}
    {% endblock %}
{% endblock %}