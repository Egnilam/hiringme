{% extends 'base.html.twig' %}

{% block title %}Wishlist{% endblock %}

{% block content %}
    {% block body %}
        <h1>Wishlist</h1>
        <a href="{{ path('wishlist_item_add', { wishlistId: wishlist.id }) }}">Add item</a>
        {% for item in wishlist.items %}
            {% include 'wishlist/wishlist_item/_show.html.twig' %}
        {% endfor %}
    {% endblock %}
{% endblock %}