<form action="{{ path('wishlist_group_delete', { 'id' : wishlist_group.id}) }}" method="post">
    <input type="hidden" name="_method" value="DELETE" />
    <input type="hidden" name="_token" value="{{csrf_token('delete.'~ wishlist_group.id)}}">
    <input type="submit" value="Delete" />
</form>