{% extends 'base.html.twig' %}

{% block title %}Create new group{% endblock %}

{% block content %}
    {% block body %}
        <h1>Associate wishlist</h1>
        <a href="{{ path('wishlist_create', { 'group' : wishlist_group_id }) }}">Create wishlist</a>
        {{ form_start(form)}}
        {{ form_widget(form) }}
        <button type="submit">Envoyer</button>
        {{ form_end(form) }}
    {% endblock %}
{% endblock %}